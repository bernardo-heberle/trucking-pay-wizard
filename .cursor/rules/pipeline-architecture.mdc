---
description: Pipeline architecture rules for src/ — dependency direction, stage contracts, OCP/ISP, traceability
globs: src/**/*.py
alwaysApply: false
---

# Pipeline Architecture

## Dependency Direction

`gui → pipeline` only. Pipeline packages (`ingestion`, `ocr`, `extraction`, `validation`, `report`) must never import from `src/gui/`. The GUI is a thin presentation layer over the pipeline.

## Report vs. Extraction

`src/report/` consumes cached extraction results and produces output artifacts. It must not perform field extraction. Extraction produces data; report assembly consumes it. Keep these concerns separate.

## Stage Contracts

Each stage has a defined contract:

- **Preconditions** — what the stage expects as input (e.g., OCR expects an image or PDF path)
- **Postconditions** — what the stage guarantees as output (e.g., extraction returns a typed dataclass or raises)
- **Invariants** — what always holds (e.g., every extracted value carries `source_document` and `source_location`)

Keep contracts narrow (ISP). A caller that only needs extracted text should not depend on a type that also carries bounding boxes and confidence scores. Split contracts when consumers only need a subset.

### Extraction contract: success vs. failure

`LlmExtractor.extract()` always returns a `DocumentExtractionResult`. It never returns `None`. It raises only for non-retryable configuration errors (bad API key, malformed request).

- **Success**: `result.extraction_error is None`, `result.fields` contains zero or more extracted fields.
- **Soft failure** (retries exhausted): `result.extraction_error` is a non-empty string, `result.fields` is empty.

**Callers must check `extraction_error` before caching.** Do not call `cache_put` when `extraction_error` is set — the document must be retried on the next pipeline run. Failed results are still appended to the results list so they appear in report output with a "review manually" notice.

```python
extraction = extractor.extract(ocr_result, page_count=ingested.page_count)
if extraction.extraction_error:
    logger.warning("Extraction failed for '{}': {}", source_path.name, extraction.extraction_error)
else:
    cache_put(folder, extraction, version=version)
results.append(extraction)
```

## Open/Closed Principle

New stages slot in without editing existing ones. Write stages that consume a well-defined input and produce a well-defined output so new stages can be inserted around them.

## Extraction

The extraction stage uses `LlmExtractor` (`src/extract/llm/extractor.py`). It receives an `OcrResult` and returns a `DocumentExtractionResult`. The extractor is instantiated with `LlmExtractor.from_config()`.

## Schema-Driven Extraction

LLM extraction is schema-driven. Each schema subclasses `ExtractionSchema` and defines the fields to extract, the tool_use JSON definition, and a parser. To support a new document type, add a schema in `src/extract/llm/schemas/` — the `LlmExtractor` does not change.

## PII Sanitization

**Never send unsanitized OCR text to an external API.** The PII sanitizer (`src/extract/llm/sanitizer.py`) must run before any text reaches the Anthropic API. This is a hard requirement — it applies regardless of provider, model, or data-handling policies.

## Traceability

Every extracted value must carry provenance. This is a hard requirement.

```python
@dataclass
class ExtractedField:
    name: str
    value: str
    source_document: str
    source_location: str | None = None
    confidence: float | None = None
```

Never return a bare value from an extraction function when a traced dataclass is expected.
