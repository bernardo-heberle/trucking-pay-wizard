# WSL-specific requirements for mutation testing.
# Identical to requirements.txt except PySide6 is omitted —
# it requires a display server that WSL does not provide,
# and src/gui/* is excluded from mutmut targets anyway.

# Data & validation
pydantic>=2.6
python-dotenv>=1.0
pandas>=2.2
openpyxl>=3.1
numpy>=1.26

# PDF handling & report assembly
pymupdf>=1.24
pikepdf>=9.0

# Image processing
opencv-python>=4.9
pillow>=10.0

# Extraction helpers
rapidfuzz>=3.6

# Progress & logging
tqdm>=4.66
loguru>=0.7

# OCR provider SDK
azure-ai-documentintelligence>=1.0
azure-identity>=1.16

# LLM extraction provider SDK
anthropic>=0.39

# --- Dev / build dependencies ---
pytest>=8.1
pytest-cov>=5.0
pytest-randomly>=3.15
pytest-mock>=3.14
hypothesis>=6.100
freezegun>=1.5
mutmut>=2.5
# pyinstaller is Windows-only — omitted in WSL
